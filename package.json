{
  "name": "spacemint-web-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --mode dev",
    "build": "npm run build:prod",
    "build:dev": "npm run validate-env -- dev && tsc -b && vite build --mode dev",
    "build:sit": "npm run validate-env -- sit && tsc -b && vite build --mode sit",
    "build:uat": "npm run validate-env -- uat && tsc -b && vite build --mode uat",
    "build:prod": "npm run validate-env -- prod && tsc -b && vite build --mode prod",
    "validate-env": "node scripts/validate-env.mjs",
    "models:dae-to-glb": "powershell -ExecutionPolicy Bypass -File ./scripts/convert-dae-to-glb.ps1",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@google/model-viewer": "^4.2.0",
    "@vitejs/plugin-react": "^5.0.4",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.15.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3"
  }
}
